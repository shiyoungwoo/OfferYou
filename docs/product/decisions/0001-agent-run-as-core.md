# ADR 0001：以 JobApplyRun 作为系统主线

## 状态

已接受。

## 背景

OfferYou 早期实现容易围绕页面和按钮推进，导致 AI 主链路、确认状态和最终快照之间出现漂移。

## 决策

每一次岗位定制都创建并维护一个 `JobApplyRun`。它记录输入、Agent 步骤、模型调用、风险提示、人工确认点和最终产物。

## 后果

- 可以复盘一份简历为什么这样生成。
- 可以在模型失败或人工确认点暂停。
- 页面不再是唯一状态来源。
- 实现需要更清晰的状态管理和测试。
