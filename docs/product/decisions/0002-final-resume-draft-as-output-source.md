# ADR 0002：FinalResumeDraft 是唯一输出源

## 状态

已接受。

## 背景

项目曾出现接受建议后预览不同步、预览模板和导出模板不一致、面试准备读取旧文本等问题。

## 决策

预览、PDF 导出、面试准备和投递记录都必须读取同一份 `FinalResumeDraft`。

## 后果

- 可以减少预览与导出不一致。
- 面试准备继承最终投递语境。
- Snapshot 合成必须严格过滤未确认内容、重复内容和模块错配。
