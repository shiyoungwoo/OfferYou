# ADR 0003：模型失败必须可见

## 状态

已接受。

## 背景

OfferYou 的核心价值来自 AI 理解、判断和改写。如果模型不可用时仍用规则兜底生成类似 AI 的结果，用户会误以为已完成岗位定制。

## 决策

必须 AI 的节点在模型失败时停止或进入人工确认。规则兜底只能用于基础编辑、开发排查和失败恢复，不能标记为 AI 改写。

## 后果

- 用户会更早看到模型配置问题。
- 低质量结果不会被伪装成可投递简历。
- 页面需要显示可理解的中文状态，而不是内部技术术语。
