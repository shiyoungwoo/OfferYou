# OfferYou 项目执行约束

## 当前冻结规则

- 当前主链路已经收口为「上传简历 / JD → 简历结构校准 → JD 定制改写建议 → 人工确认 → 同步预览 → 导出 PDF」。
- 当前简历模板视为稳定资产：`Professional CN` 与 `ATS Clean` 的结构、颜色、导出一致性默认不再修改。
- 后续除非用户明确要求「修改模板 / 改排版 / 改颜色 / 改导出样式」，不要主动调整简历模板。
- 后续优化优先放在 AI 改写质量、PDF 解析稳定性、字段识别准确率、建议接受后的同步一致性。

## AI 与代码职责边界

- OfferYou 是 Agent-first 产品，不是规则工作流外面套一层 AI。
- 需要理解、比较、判断、取舍、改写的节点必须由 AI 主导，包括天赋挖掘、JD 理解、简历结构校准、匹配度与差距分析、改写策略、改写建议、语义事实校验和面试准备。
- 需要稳定、保存、状态、工具调用、导出、安全和一致性检查的节点必须由代码主导，包括文件上传、类型校验、OpenDataLab PDF 调用、数据库读写、JobApplyRun 状态机、FinalResumeDraft 合成、预览、PDF 导出和日志脱敏。
- 天赋挖掘不是可选增强。`TalentProfile` 是 OfferYou 最大能力模式的核心上下文。未完成天赋挖掘时，可以进入基础岗位定制；完成天赋挖掘后，才进入天赋驱动 Agent 模式。
- 模型不可用时，只允许进入基础编辑 / 人工确认 / 规则参考状态，不允许把规则兜底内容标记为「AI 优化改写」。
- JD 输入模式只保留三档：基础模式、标准 AI、高质量 AI。默认使用标准 AI；截图 JD 不能只用纯 OCR 文本判断公司和岗位，必须保留版面/坐标信息并要求用户确认当前选中岗位。
- 小米 MiMo 分层：普通文本节点默认使用 `mimo-v2.5`，复杂推理节点默认使用 `mimo-v2.5-pro`，截图/视觉识别预留 `mimo-v2.5`。OpenAI Codex OAuth 分层：简单节点默认 `gpt-5.4-mini`，复杂节点默认 `gpt-5.5`，实际模型名可由环境变量覆盖。
- 详细边界以 `docs/plans/2026-05-07-ai-code-boundary-talent-first-plan.md` 为准。

## 验证要求

- 修改预览或导出链路后，至少运行 `pnpm exec tsc --noEmit`。
- 修改模板后，至少运行预览相关测试，并用真实浏览器截图确认 `Professional CN` 与 `ATS Clean`。
- 不提交 `.env.local`、`storage/`、`.next/`、`node_modules/`、日志、SQLite 数据库和导出 PDF。
